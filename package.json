{
  "name": "image-keeper",
  "private": true,
  "version": "1.0.0",
  "description": "Image storage. Upload and view saved images.",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build:server": "pnpm --filter server build --no-frozen-lockfile",
    "build:web": "pnpm --filter web build --no-frozen-lockfile",
    "start:server": "pnpm --filter server start:prod",
    "start:web": "pnpm --filter web start"
  },
  "prisma": {
    "schema": "packages/database/schema.prisma"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/potyoma/image-uploader.git"
  },
  "keywords": [
    "next",
    "nest",
    "react",
    "typescript"
  ],
  "author": "potyoma",
  "license": "ISC",
  "devDependencies": {
  }
}
